{
  "id": "course.check_conflicts",
  "description": "Check if a course conflicts with your current schedule",
  "parameters": {
    "type": "object",
    "properties": {
      "course_id": {
        "type": "string",
        "description": "The course ID to check for schedule conflicts"
      }
    },
    "required": ["course_id"]
  }
}
