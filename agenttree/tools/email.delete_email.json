{
  "id": "email.delete_email",
  "description": "Delete an email",
  "parameters": {
    "type": "object",
    "properties": {
      "email_id": {
        "type": "string",
        "description": "ID of the email to delete"
      }
    },
    "required": ["email_id"]
  }
}
