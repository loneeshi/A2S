{
  "id": "course.search",
  "description": "Search for courses by keyword or topic",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search keywords (course name, topic, or code)"
      },
      "department": {
        "type": "string",
        "description": "Filter by department name"
      }
    },
    "required": ["query"]
  }
}
