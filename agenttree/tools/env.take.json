{
  "id": "env.take",
  "name": "Take",
  "description": "Pick up an object from a receptacle",
  "category": "manipulation",
  "parameters": {
    "object": {
      "type": "string",
      "description": "The object to pick up (e.g. 'apple 1')",
      "required": true
    },
    "receptacle": {
      "type": "string",
      "description": "The receptacle to take the object from (e.g. 'countertop 2')",
      "required": true
    }
  }
}
