{
  "id": "env.toggle",
  "name": "Toggle",
  "description": "Toggle an object on or off (lamp, stove, etc.)",
  "category": "interaction",
  "parameters": {
    "object": {
      "type": "string",
      "description": "The object to toggle (e.g. 'desklamp 1')",
      "required": true
    }
  }
}
