{
  "id": "env.heat",
  "name": "Heat",
  "description": "Heat an object using a microwave",
  "category": "manipulation",
  "parameters": {
    "object": {
      "type": "string",
      "description": "The object to heat (e.g. 'egg 1')",
      "required": true
    },
    "receptacle": {
      "type": "string",
      "description": "The microwave to use (e.g. 'microwave 1')",
      "required": true
    }
  }
}
