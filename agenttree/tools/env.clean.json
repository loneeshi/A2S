{
  "id": "env.clean",
  "name": "Clean",
  "description": "Clean an object using a sink basin",
  "category": "manipulation",
  "parameters": {
    "object": {
      "type": "string",
      "description": "The object to clean (e.g. 'plate 1')",
      "required": true
    },
    "receptacle": {
      "type": "string",
      "description": "The sink basin to use (e.g. 'sinkbasin 1')",
      "required": true
    }
  }
}
