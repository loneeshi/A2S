{
  "id": "geography.walk_to",
  "description": "Walk along a calculated path to a destination",
  "parameters": {
    "type": "object",
    "properties": {
      "path_info": {
        "type": "object",
        "description": "Path object returned by map.find_optimal_path"
      }
    },
    "required": ["path_info"]
  }
}
