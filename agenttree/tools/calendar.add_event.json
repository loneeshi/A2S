{
  "id": "calendar.add_event",
  "description": "Add an event to a calendar. Use 'self' for personal calendar or email address for others.",
  "parameters": {
    "type": "object",
    "properties": {
      "calendar_id": {
        "type": "string",
        "description": "Calendar ID: 'self' for personal calendar, or email address for others"
      },
      "event_title": {
        "type": "string",
        "description": "Event title"
      },
      "location": {
        "type": "string",
        "description": "Event location"
      },
      "time": {
        "type": "string",
        "description": "Event time in format 'Week X, Day, HH:MM-HH:MM' (e.g., 'Week 3, Monday, 15:00-16:00')"
      },
      "description": {
        "type": "string",
        "description": "Optional event description"
      }
    },
    "required": ["calendar_id", "event_title", "location", "time"]
  }
}
