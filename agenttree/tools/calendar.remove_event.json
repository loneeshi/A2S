{
  "id": "calendar.remove_event",
  "description": "Remove an event from a calendar",
  "parameters": {
    "type": "object",
    "properties": {
      "calendar_id": {
        "type": "string",
        "description": "Calendar ID"
      },
      "event_id": {
        "type": "string",
        "description": "Event ID to remove"
      }
    },
    "required": ["calendar_id", "event_id"]
  }
}
