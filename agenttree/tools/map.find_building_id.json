{
  "id": "map.find_building_id",
  "description": "Find a campus building's ID by its name or alias",
  "parameters": {
    "type": "object",
    "properties": {
      "building_name": {
        "type": "string",
        "description": "The building name or alias to look up (e.g., 'Grand Central Library', 'Science Hall')"
      }
    },
    "required": ["building_name"]
  }
}
