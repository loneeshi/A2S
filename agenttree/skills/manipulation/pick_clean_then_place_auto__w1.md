---
description: >-
  Auto-generated skill for pick_clean_then_place tasks based on 2 failure
  reflections
whenToUse: >-
  When handling pick_clean_then_place tasks, especially to avoid:
  timeout:iteration_limit
steps:
  - 分析任务目标，确认需要的工具和操作序列
  - 观察环境状态，定位目标物体
  - 执行操作并验证每步结果
  - 确认任务完成
tags:
  - manipulation
  - pick_clean_then_place
  - auto-generated
---
## 避免任务超时的技能指南

在执行“pick_clean_then_place”任务时，代理可能会遇到超时失败。以下是优化任务执行的步骤和策略。

## 工作流程

1. **分析任务**  
   确认任务的每个步骤，包括拾取、清理和放置。确保对每个步骤的理解清晰。

2. **优化执行策略**  
   - 使用 `env.goto` 迅速移动到目标位置，减少不必要的移动时间。
   - 在执行 `env.take` 之前，确保目标物体在可达范围内。

3. **有效清理**  
   - 在执行 `env.clean` 时，确保选择最有效的清洁工具，避免重复清理。
   - 设定清理的优先级，集中在最脏的区域。

4. **高效放置**  
   - 使用 `env.place` 时，确保目标位置是空的，并且在可达范围内。
   - 规划放置顺序，避免在放置过程中出现干扰。

5. **迭代优化**  
   - 记录每次任务的执行时间，找出瓶颈并进行调整。
   - 定期回顾并优化任务策略，确保每次执行都在最大迭代次数内完成。

## 常见错误

- **错误选择工具**  
  确保在每一步中使用适合的工具，避免因工具不当导致的时间浪费。

- **忽视环境**  
  在执行任务时，未考虑周围环境的影响，导致路径不畅或物体干扰。

- **任务规划不清晰**  
  在开始任务前未进行充分的规划，导致执行过程中频繁调整。

## 恢复策略

- **监控任务进度**  
  如果任务接近超时，立即检查当前步骤和环境，快速调整策略。

- **中断并重试**  
  在任务超时的情况下，及时中断当前任务，分析失败原因后重新开始。

- **使用回退机制**  
  如果某一步骤失败，使用回退机制返回到上一个成功的步骤，重新评估并尝试。

通过遵循上述工作流程和避免常见错误，代理可以有效减少“pick_clean_then_place”任务中的超时失败。
