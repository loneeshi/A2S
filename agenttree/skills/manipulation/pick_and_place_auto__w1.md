---
description: Auto-generated skill for pick_and_place tasks based on 4 failure reflections
whenToUse: >-
  When handling pick_and_place tasks, especially to avoid:
  timeout:iteration_limit
steps:
  - 分析任务目标，确认需要的工具和操作序列
  - 观察环境状态，定位目标物体
  - 执行操作并验证每步结果
  - 确认任务完成
tags:
  - manipulation
  - pick_and_place
  - auto-generated
---
## 避免超时错误的技能指南

在执行“pick_and_place”任务时，超时错误是常见的问题。以下是避免这些失败的步骤和策略。

## 工作流程

1. **任务规划**
   - 确定任务的每个步骤，包括物体的位置和目标位置。
   - 使用 `env.goto` 确保代理能够准确到达目标位置。

2. **优化执行策略**
   - 在执行每个步骤前，使用 `env.check` 确保环境状态符合预期。
   - 使用 `env.take` 准确抓取物体，避免不必要的重复尝试。

3. **迭代限制调整**
   - 根据任务复杂性，适当调整最大迭代次数，确保更复杂的操作有足够的时间完成。
   - 在任务开始前，使用 `env.set_max_iterations` 设置合理的迭代上限。

4. **监控执行过程**
   - 在任务执行过程中，定期检查进度，确保没有步骤被遗漏。
   - 使用 `env.status` 监控任务状态，及时发现潜在问题。

## 常见错误

- **忽视环境检查**
  - 在执行操作前未使用 `env.check`，导致环境状态不符合预期。
  
- **固定的迭代次数**
  - 对所有任务使用相同的最大迭代次数，未考虑任务复杂性。

- **缺乏步骤规划**
  - 未明确每个步骤，导致任务执行时出现混乱。

## 恢复策略

1. **任务中断**
   - 如果任务超时，立即使用 `env.abort` 中止当前操作，避免进一步的资源浪费。

2. **分析失败原因**
   - 记录超时的具体步骤，使用 `env.log` 进行详细分析，以找出问题所在。

3. **调整策略**
   - 根据失败原因，修改任务规划或优化执行策略，确保下次执行更顺利。

4. **重新尝试**
   - 在调整完毕后，使用 `env.retry` 重新开始任务，确保新策略的有效性。

通过遵循上述步骤和策略，您可以显著减少在“pick_and_place”任务中遇到的超时错误，提高操作的效率和成功率。
