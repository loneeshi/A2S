---
description: Auto-generated skill for pick_and_place tasks based on 3 failure reflections
whenToUse: >-
  When handling pick_and_place tasks, especially to avoid:
  timeout:iteration_limit
steps:
  - 分析任务目标，确认需要的工具和操作序列
  - 观察环境状态，定位目标物体
  - 执行操作并验证每步结果
  - 确认任务完成
tags:
  - manipulation
  - pick_and_place
  - auto-generated
---
## 避免超时失败的技能指南

在“操控”领域中，成功完成“捡起和放置”任务至关重要。以下是避免超时失败的工作流程、常见错误及恢复策略。

## 工作流程

1. **任务规划**
   - 在开始任务前，使用 `env.goto` 确定目标位置。
   - 评估周围环境，识别潜在障碍。

2. **优化动作序列**
   - 使用 `env.take` 和 `env.place` 进行动作时，确保动作顺序合理。
   - 预先模拟动作序列，确保每个步骤都有效。

3. **迭代监控**
   - 在每次迭代中，检查任务进度，确保在最大允许迭代内完成。
   - 如果发现进度缓慢，及时调整策略。

4. **工具使用**
   - 确保所用工具适合当前任务，避免因工具不当导致的效率低下。
   - 定期检查工具状态，确保其正常工作。

## 常见错误

- **未优化动作序列**
  - 预防规则：在执行任务前，务必模拟并优化动作序列，确保每一步都有明确目的。

- **忽视环境障碍**
  - 预防规则：在任务开始前，使用 `env.goto` 评估环境，确保没有未被识别的障碍物。

- **未监控迭代进度**
  - 预防规则：在每次迭代中，记录进度并与预期时间进行对比，及时调整策略。

## 恢复策略

1. **识别问题**
   - 如果任务超时，首先分析导致超时的原因，是动作序列不当还是环境障碍。

2. **调整策略**
   - 根据识别的问题，重新规划任务。可以使用 `env.goto` 重新评估目标位置，或调整动作顺序。

3. **分解任务**
   - 如果任务复杂，考虑将其分解为更小的子任务，逐步完成。

4. **增加迭代次数**
   - 如果合理，适当增加最大允许迭代次数，以允许更多尝试。

通过遵循以上指南，您将能够有效减少“捡起和放置”任务中的超时失败，提高操作效率。
