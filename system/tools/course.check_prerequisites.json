{
  "id": "course.check_prerequisites",
  "description": "Check prerequisite requirements for a course",
  "parameters": {
    "type": "object",
    "properties": {
      "course_id": {
        "type": "string",
        "description": "The course ID to check prerequisites for"
      }
    },
    "required": ["course_id"]
  }
}
