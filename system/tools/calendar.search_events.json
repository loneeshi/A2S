{
  "id": "calendar.search_events",
  "description": "Search calendar events by query or date",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search keywords for event titles"
      },
      "date": {
        "type": "string",
        "description": "Filter events by date"
      }
    },
    "required": []
  }
}
