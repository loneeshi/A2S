{
  "id": "env.look",
  "name": "Look",
  "description": "Look around the current environment or examine a specific object/location",
  "category": "perception",
  "parameters": {
    "target": {
      "type": "string",
      "description": "Optional target to look at. If empty, looks around the room.",
      "required": false
    }
  }
}
